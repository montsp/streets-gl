# 商用利用およびビジュアル強化戦略 (Commercial & Visual Strategy)

## 1. 商用利用とライセンスについて

### 結論
現在のライセンス戦略（MIT, LGPL, ODbL 等）は、**広告表示やサブスクリプションなどの商用利用を完全に許容しています。** 「商用利用を考えていない」ということは全くありません。

### 各ライセンスの商用利用における詳細
- **Streets GL (MIT)**: 商用・非商用を問わず、自由に利用、改変、配布が可能です。広告の表示や有料化に制限はありません。
- **JSBSim (LGPL)**: 物理計算エンジンを WASM モジュールとして「動的リンク（実行時ロード）」形式で利用するため、アプリ本体を MIT のまま商用利用することが可能です。
- **OpenStreetMap (ODbL)**: 商用利用は可能ですが、クレジット表記（Attribution）が必須です。また、地図データ自体を改変した場合はそのデータを公開する義務がありますが、アプリ本体のソースコードを公開する義務はありません。
- **機体アセット (CC-BY / CC0)**: 多くのオープンソースアセットは商用利用を許可しています。制作者のクレジットを表示することで、商用シミュレーターの一部として統合可能です。

### 1.1 FlightGear の 3D モデル（GPL アセット）の取り扱い
FlightGear の公式リポジトリにある機体モデルの多くは **GPL (GNU General Public License)** でライセンスされています。これらを採用する場合、以下の対応が必要です。
- **ライセンスの隔離**: GPL アセットを MIT ライセンスのコードと「静的に結合」することはできません。そのため、機体モデルは独立した「データファイル（.glb 等）」として管理し、アプリ実行時に Streets GL のエンジンがそれらを読み込む「アグリゲート（集合体）」という形態をとります。これにより、Streets GL 本体が GPL になることを防ぎます。
- **フォーマット変換**: FlightGear のモデルは AC3D 形式（.ac）であることが多いため、Blender 等を介して高度な glTF/GLB 形式に変換し、PBR（物理ベースレンダリング）対応のテクスチャを再設定する必要があります。
- **派生物の公開**: 機体モデル自体を改変（メッシュの変更やテクスチャの修正）した場合、その機体アセットに関しては GPL に基づき改変後のデータを公開する義務が生じます。

---

## 2. 衛星写真と Streets GL の融合（ハイブリッド・レンダリング）

Streets GL のベクトルベースの表現に衛星写真を融合させることで、リアリティを飛躍的に向上させる「ハイブリッド・レンダリング」を提案します。

### 実装アプローチ
1.  **ベース・テクスチャとしての衛星写真**:
    - 地形（Terrain）のシェーダーにおいて、現在の単色または汎用テクスチャの代わりに、Esri World Imagery や Mapbox Satellite などの衛星写真タイルをサンプリングして描画します。
2.  **OSM データによる情報の補完（ブレンド）**:
    - 衛星写真は解像度に限界があり、拡大するとボケてしまいます。
    - **道路・建物敷地**: OSM のベクトルデータから生成した高解像度テクスチャを衛星写真の上に重ねて描画することで、滑走路のラインや道路の境界をくっきりと表現します。
    - **植生・水域**: 衛星写真の色彩を活かしつつ、OSM のタグ情報に基づいた動的なエフェクト（水の揺らぎ、木々の配置）を組み合わせます。

### 利点
- **遠景のリアリティ**: 高空からの視点では、衛星写真によるリアルな地形の色彩が支配的になります。
- **近景の精細さ**: 低空飛行時や離着陸時には、OSM 由来の 3D 建物や高精細な滑走路テクスチャがディテールを補い、ボケを防ぎます。
- **没入感の向上**: 衛星写真に含まれる「地面の汚れ」「影」「植生の密度」などが、抽象的なベクトルデータにはない「実在感」をもたらします。

### 2.1 衛星写真データのソース候補
リアリティ向上のための衛星写真タイル（WMTS/TMS）は、以下のソースから取得を検討します。

1.  **Esri World Imagery**:
    - **特徴**: 非常に高精細で、Streets GL が現在地形データ（Terrain 3D）として利用している ArcGIS と親和性が高いです。
    - **ライセンス**: 開発・教育目的など、特定の条件下での利用が比較的寛容です。
2.  **Mapbox Satellite**:
    - **特徴**: 最新の衛星写真が提供されており、タイルの配信速度が非常に高速です。
    - **コスト**: 商用利用（広告表示を含む）の場合、月間のリクエスト数に応じた従量課金が発生します。
3.  **Sentinel-2 (Copernicus)**:
    - **特徴**: 完全なオープンデータであり、無料で利用可能です。
    - **制限**: 解像度が 10m/px 程度と低いため、高空からの「遠景用」としての利用に適しています。
4.  **Community-driven (OpenAerialMap)**:
    - **特徴**: 有志によって撮影された航空写真リポジトリです。一部の地域では超高解像度ですが、全球をカバーしているわけではありません。

### 技術的課題と対策
- **位置のズレ（アライメント）**: OSM データと衛星写真には数メートルのズレが生じることがあります。
    - **対策**: 滑走路などの重要なエリアでは、OSM データを優先して描画し、周囲の衛星写真をマスク処理で馴染ませるアルゴリズムを導入します。
- **通信負荷**: 衛星写真タイルの読み込みにより、データ通信量が増加します。
    - **対策**: 高度に応じて必要な解像度を動的に選択（LOD 管理）し、先行読み込み（プリフェッチ）を最適化します。
