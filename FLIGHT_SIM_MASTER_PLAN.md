# Streets GL フライトシミュレーター ホワイトペーパー (Master Plan)

## 1. Executive Summary: なぜ今、Streets GLでフライトシムなのか

### ビジョン
「インストール不要、ブラウザを開いて5秒で、地球上のあらゆる場所を自らの手で操縦し、探索できる体験」を提供します。OpenStreetMap (OSM) の詳細な3D建物データと、業界標準の航空力学エンジンを融合させ、次世代の「Webベース・VFR（有視界飛行）シミュレーター」を構築します。

### 市場における位置付けと差別化 (Pain Point Resolution)
既存のシミュレーター（MSFS, GeoFS, Infinite Flight）と比較し、Streets GL版は以下の「不便」を解消します。

| 比較項目 | Microsoft Flight Sim (MSFS) | Infinite Flight | **Streets GL Sim (本案)** |
| :--- | :--- | :--- | :--- |
| **Click-to-Fly Time** | 5〜10分 (重い起動) | 2〜3分 | **5秒 (ブラウザで即座に)** |
| **初期コスト** | 高価なPC + ソフト代 | サブスクリプション | **無料 (広告/OSM貢献重視)** |
| **ストレージ** | 200GB以上 | 数GB | **0GB (ストリーミング)** |
| **得意分野** | 全世界・超高画質 | プロフェッショナルIFR | **VFR観光・都市探索・低空飛行** |

**ターゲット層**:
- 「自分の家の上を飛んでみたい」というカジュアルな一般ユーザー。
- 観光地や都市部を低空で詳細に探索したいVFR（有視界飛行）愛好家。
- 教育機関や都市計画での視覚的シミュレーション。

---

## 2. Tech Stack & Innovation: 技術的優位性

### 航空力学 × WebAssembly
- **FDM (Flight Dynamics Model)**: NASAやFlightGearでも採用されている業界標準エンジン **JSBSim** をWASMで動作させます。
- **マルチスレッド計算**: 物理計算を Web Worker に分離し、描画フレームレートに影響を与えずに120Hz以上の精度で挙動を計算します。

### レンダリング & データソース
- **OSM 3D Buildings**: Streets GLの強みを活かし、テクスチャのみの地面ではなく、形状が正確な「生きた都市」を再現。
- **Next-Gen WebGL2**: PBR (物理ベースレンダリング) に加え、SSR、SSAO、リアルタイム・ボリュメトリック雲を実装。

---

## 3. Core Challenges & Solutions: 技術的課題と解決策

### 3.1 空港グラフィックの刷新 (Airport Fidelity)
現在の Streets GL の課題である「空港がただのビルに見える」問題を、専用の生成ロジックで解決します。
- **ターミナルの特化描画**: `aeroway=terminal` タグを検出し、通常の窓だらけの壁ではなく、空港特有の大型ガラスパネル、コンクリート、サインボードを含むマテリアルを動的に適用します。
- **ボーディングブリッジ (Jet Bridge)**: `aeroway=jet_bridge` から、機体のドア位置とターミナルのゲートを繋ぐプロセージャルモデルを生成。または、OSMのノード位置に高品質なインスタンスモデルを配置します。
- **空港専用アセット**: 空港特有の GSE (地上支援機材) や灯火システム (PAPI, REIL) をOSMタグから動的に配置。

### 3.2 座標精度と浮動原点 (Floating Origin) 戦略
地球規模の座標を扱う際の浮動小数点精度の問題を RTC (Relative To Center) 手法で解決します。
- **Floating Origin**: カメラの位置を常に仮想空間の原点 (0,0,0) に保ち、世界の方を動かすことで、機体周辺のガタつき（ジッター）を完全に排除します。
- **JSBSim Sync**: JSBSimが出力する ECEF (地球中心・地球固定) 座標を、レンダラーの浮動原点座標系に高精度で変換し、同期させます。

### 3.3 地形スムージングと滑走路補正 (Runway Flattening)
- **空港平坦化アルゴリズム**: `aeroway=runway` の領域において、周囲の地形データの高さ情報を無視またはスムージングし、滑走路が波打つ問題を強制的に解決します。
- **傾斜滑走路への対応**: 将来的には、OSMの傾斜データに基づき、物理エンジン側でも正確な勾配を再現します。

### 3.4 高速移動対応タイルローダー (Conical Streaming)
- **速度ベクトル予測**: 現在の視錐台 (Frustum) に加え、機体の速度ベクトルに基づいた「円錐形 (Conical) カリング」を採用し、進行方向のタイルを優先的に低LODから順次読み込みます。

### 3.5 衝突判定 (Collision Detection) の軽量化
- **自機周辺限定 B-Box**: 数百万のビルすべてを判定対象にするのではなく、自機の周囲数キロメートル以内のビルのみを対象に、簡略化された衝突判定用バウンディングボックスを動的に生成します。

### 3.6 3Dコックピット (glTF Binding)
- **glTF拡張仕様**: 計器類（計器の針、スイッチ、ヨーク）を glTF モデルのノード名として定義。
- **Property Binding**: `node_yoke_pitch` → `controls/elevator-cmd-norm` のように、JSBSimのプロパティとモデルの回転・移動を JSON マッピングで接続します。

### 3.7 航空機3Dモデル・アセット戦略
- **オープンライセンス・リポジトリの活用**: 既存の `B777`, `Cessna 208` 等のモデルをベースに、FlightGearリポジトリ等のオープンな高品質glTFモデルを順次統合します。
- **マルチレベルLOD**:
    - **Ultra-High**: コックピット視点用。すべてのスイッチ、ノブ、計器が物理的にモデル化。
    - **High**: 自機の外部視点用。翼のしなりや動翼のアニメーションを再現。
    - **Low-Poly**: 他機（マルチプレイヤー）用。描画負荷を最小限に抑えつつ、シルエットを維持。
- **動的リバリ（塗装）システム**: 1つの3Dモデルに対し、glTFのテクスチャ差し替え機能を用いて、航空会社ごとの塗装を動的に適用します。
- **物理アニメーションの同期**: JSBSimからの出力（舵角、エンジン回転数、脚の伸縮率）を、毎フレームglTFのボーン/ノードのトランスフォームに反映させます。

---

## 4. Roadmap & Milestones

### Phase 1: Foundation (成功定義: 安定した飛行と操作)
- JSBSim WASM の統合。
- 基本的なフライトコントロール（キーボード、マウス、タッチ対応）。
- **KPI**: 60fpsの維持、JSBSim計算周波数 120Hz以上。

### Phase 2: Environment & Airport (成功定義: 空港としてのリアリティ)
- 空港平坦化ロジックの実装。
- ターミナルおよびボーディングブリッジの専用描画システムの導入。
- **KPI**: 主要10空港でのグラフィック向上、滑走路上の物理挙動の安定。

### Phase 3: Avionics & HUD (成功定義: 目的地へのナビゲーション)
- HUD (Heads-Up Display) および基本的な3Dコックピット表示。
- METAR連携によるリアルタイム気象。
- **KPI**: 目的地検索と自動操縦（LNAV）の成功。

### Phase 4: Mission & Community (成功定義: ゲーム性の確立)
- OSMデータ駆動型ミッション（救急搬送、観光案内）。
- マルチプレイヤー同期 (WebRTC)。
- **KPI**: 同時接続 50名以上、ユーザー生成ミッションの公開。

---
作成者: Jules (Streets GL Flight Sim Project Team)
