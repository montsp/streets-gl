# Streets GL フライトシミュレーター 開発計画書

## 1. はじめに
本ドキュメントは、Streets GLを基盤とした高度なフライトシミュレーターの構築に向けた開発計画をまとめたものである。Streets GLが持つ強力な3Dレンダリング能力と、広大なOpenStreetMapデータ、地形データを最大限に活用し、あらゆるデバイスで動作する高品質なシミュレーション体験の実現を目指す。

## 2. 高品質なフライトシミュレーターに必要な要素

高品質な体験を提供するために、以下の要素を段階的に実装する。

### A. 航空力学モデル (Flight Dynamics Model - FDM)
単なる移動ではなく、物理法則に基づいた挙動。
- **JSBSim (WebAssembly)** の採用: FlightGear等の本格的なシミュレーターで使用されている業界標準のFDMエンジンをWASMにコンパイルして統合する。これにより、揚力、抗力、推力、重力、慣性モーメントの極めて正確な計算が可能になる。
- **機体固有のXML定義**: 各機体の性能特性をJSBSim標準のXMLフォーマットで定義し、高度なカスタマイズを可能にする。

### B. 環境とデータ
- **データソースの具体化**:
    - **地形・建物**: Streets GL既存のPlanetilerベースのOSMベクトルタイルを使用。
    - **航空データ (NAVData)**: `OurAirports` (オープンデータ) から空港、滑走路、無線標識のデータを取得。
    - **気象 (Weather)**: `checkwx.com` または `OpenWeatherMap API` からリアルタイムのMETAR情報を取得。
- **空港データの詳細化**: OSMの `aeroway` タグから滑走路の材質、方向、長さを解析し、正確なマーキングとPAPI等の灯火システムを動的に生成する。

### C. アビオニクスとUI (HUD)
- **主要飛行計器**: 速度計、高度計、昇降計、水平儀、方位計。
- **ナビゲーション**: 現在位置の座標、目的地までの距離、ILS (計器着陸装置) のシミュレーション。

## 3. アーキテクチャ設計

### 使用ライブラリ・技術スタック案
- **FDMエンジン**: `JSBSim` (WASM版)
- **入力処理**: `Web Gamepad API` (ジョイスティック対応), `Pointer Events API` (タッチ対応)
- **オーディオ**: `Web Audio API` (ドップラー効果や空間オーディオを考慮したエンジン音)
- **通信 (マルチプレイヤー)**: `WebRTC` (低遅延な機体位置同期)

現在の Streets GL の設計を拡張し、以下のコンポーネントを導入する。

### Input Abstraction Layer (入力抽象化レイヤー)
現在の Streets GL はマウスとキーボードのイベントに強く依存しており、これがマルチデバイス対応の妨げとなっている。
- **InputSystem の新設**: キーボード、マウス、タッチ、ゲームパッド、さらにはジャイロスコープの入力を「Pitch」「Roll」「Yaw」「Throttle」といった抽象的なコマンドに変換する。
- **Input Axes & Buttons**:
  - Axes: `pitch`, `roll`, `yaw`, `throttle` (各 -1.0 〜 1.0 または 0.0 〜 1.0)
  - Buttons: `brakes`, `flaps`, `gear`, `cycleCamera`
- **Device Adapters**: 各デバイス固有のイベント（KeyboardEvent, TouchEvent, Gamepad API）を抽象コマンドにマッピングする。

```typescript
// InputSystem のインターフェース案
interface FlightInputState {
    axes: {
        pitch: number;    // -1.0 (Down) to 1.0 (Up)
        roll: number;     // -1.0 (Left) to 1.0 (Right)
        yaw: number;      // -1.0 (Left) to 1.0 (Right)
        throttle: number; // 0.0 (Idle) to 1.0 (Full)
    };
    buttons: {
        brakes: boolean;
        flaps: boolean;
        gear: boolean;
    };
}
```

### FlightSystem (飛行システム)
- プレイヤーの機体の状態（位置、速度、姿勢、燃料、エンジンの状態など）を管理する。
- 毎フレーム FDM を更新し、機体の新しい座標と姿勢を計算する。

## 4. マルチデバイス・マルチスペック対応戦略

### タッチデバイスへの対応 (Operation Design)
現在の「PC前提」の操作を根本から見直す。
- **デュアル・バーチャルスティック**:
  - 左スティック: スロットル（上下）とヨー（左右）。
  - 右スティック: ピッチ（上下）とロール（左右）。
- **スロットル・ホールド**: 指を離しても出力を維持するスライダー形式。
- **傾斜操作 (Tilt Controls)**: デバイスを傾けることで直感的にロールとピッチを操作可能にする（オプション）。
- **フローティングUI**: 操縦に集中できるよう、HUDは半透明かつシンプルに構成。

### スペック面での対応 (Performance Optimization)
- **LOD (Level of Detail) の強化**: モバイル端末では建物の描画距離を動的に制限。
- **テクスチャ・シェーダーの最適化**: 端末の性能に応じて、ポストプロセス（SSAO, SSRなど）の強度を自動調整または無効化。
- **レンダリング解像度のスケーリング**: 高負荷時に内部解像度を下げ、フレームレートを維持。

## 5. 実装ロードマップ

1.  **Phase 1: 基礎インフラの整備**
    - `InputSystem` の実装と入力の抽象化。
    - タッチイベント (`touchstart`, `touchmove`, `touchend`) のサポート。
    - モバイルアクセス制限の解除と基本的な動作確認。

2.  **Phase 2: 基本飛行機能の実装**
    - 簡易的な物理モデルの導入。
    - プレイヤー機体の 3D モデルの描画。
    - カメラシステムの追従機能 (Follow Camera) の実装。

3.  **Phase 3: 高度なシミュレーションとUI**
    - 物理モデルの精緻化 (FDM)。
    - HUD (Heads-Up Display) の実装。
    - ゲームパッド (Web Gamepad API) への対応。

4.  **Phase 4: ディテールと最適化**
    - 空港の詳細描画。
    - 環境効果 (風、雲) の追加。
    - 各種デバイス向けのパフォーマンス・チューニング。

## 6. 他のシミュレーターとの差別化ポイント (Unique Selling Points)

既存の GeoFS や Infinite Flight に対し、以下の点で圧倒的な差別化を図る。

- **超高精細な都市景観**: 他のシミュレーターが「テクスチャだけの地面」であるのに対し、Streets GLの強みを活かし、数百万のOSMビルを詳細にレンダリングする。これにより、ニューヨークや東京などの大都市での「ビル間飛行」や「観光飛行」において比類なき体験を提供する。
- **OSM駆動型ミッション**: OSMから「スタジアム」「屋上ヘリポート」「公園」などを検出し、「特定の施設への救急搬送ミッション」や「ランドマーク巡り」を動的に生成する。
- **高度なグラフィックスのアクセシビリティ**: インストール不要のブラウザベースでありながら、SSR（画面空間反射）やSSAOなどの最新技術により、既存のモバイル・ブラウザシミュレーターを超える視覚的没入感を提供する。

## 7. 懸念点と課題
- **OSMデータの整合性**: 滑走路の高さが地形データと完全に一致しない場合があり、離着陸時の挙動に影響する可能性がある。
- **ネットワーク遅延**: 地形データの読み込みが追いつかない場合の高速飛行時の処理（タイルの先読みアルゴリズムの改善が必要）。
- **ブラウザの制限**: モバイルブラウザでのメモリ制限や WebGL2 の互換性問題。

---
作成者: Jules (Streets GL Development AI)
